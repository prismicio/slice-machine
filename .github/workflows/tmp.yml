name: ci tmp

on:
  push:
    branches:
      - dev-plugins
  pull_request:
    branches:
      - dev-plugins

jobs:
  ci:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [14]

    steps:
      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}

      - name: Checkout
        uses: actions/checkout@master

      - name: Install dependencies
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci

      - name: Lint
        run: cd packages/init && npm run lint

      - name: Unit
        run: cd packages/init && npm run unit

      - name: Build
        run:cd packages/init &&  npm run build