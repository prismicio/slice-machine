import { sprinkles } from "@prismicio/editor-ui";
import { style } from "@vanilla-extract/css";

export const environmentDivider = style([
  sprinkles({
    marginTop: 16,
    marginBottom: 32,
  }),
]);
