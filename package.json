{
  "name": "root",
  "private": true,
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --filter=!e2e-project-next2",
    "audit": "turbo run audit",
    "lint": "turbo run lint",
    "lint-staged": "lint-staged",
    "format": "turbo run format",
    "test": "turbo run test --filter=!e2e-project-next2",
    "test:e2e": "npm run cypress-setup && start-server-and-test 'npm run dev --prefix e2e-projects/cypress-next-app' http://localhost:3000 'SM_ENV=development NO_SENTRY=1 npm run slicemachine --prefix e2e-projects/cypress-next-app' http://localhost:9999 'cypress run'",
    "test:e2e:dev": "npm run cypress-setup && start-server-and-test 'npm run dev --prefix e2e-projects/cypress-next-app' http://localhost:3000 'SM_ENV=development NO_SENTRY=1 npm run slicemachine --prefix e2e-projects/cypress-next-app' http://localhost:9999 'cypress open'",
    "::changeset": "npm run changeset add && git add .changeset && git commit -m '[release:changelog] update changeset'; echo 'We commited your changeset. You should defo push this master ✌️'",
    "::release": "manypkg check && npm run build && lerna publish && npm run run changeset",
    "bump:interactive": "lerna version prerelease --preid alpha --no-push --exact",
    "bump:alpha": "lerna version prerelease --preid $npm_config_preid --no-changelog --exact --yes",
    "publish:alpha": "lerna publish from-package --dist-tag alpha --no-verify-access --yes",
    "publish:production": "PUBLIC_SM_UI_SEGMENT_KEY=cGjidifKefYb6EPaGaqpt8rQXkv5TD6P PUBLIC_SM_INIT_SEGMENT_KEY=ED3O1FVsa8DteNyRmAuDyMcPoftJ6VdP lerna publish from-package",
    "bump:production": "lerna version --conventional-graduate --exact",
    "depcheck": "depcheck --skip-missing; turbo run depcheck",
    "lerna:update": "npx --yes lerna-update-wizard"
  },
  "devDependencies": {
    "depcheck": "^1.4.3",
    "husky": "^7.0.2",
    "lerna": "^3.22.1",
    "lint-staged": "^11.1.2",
    "turbo": "^1.8.2"
  },
  "dependencies": {
    "cypress": "^12.3.0",
    "cypress-localstorage-commands": "^2.2.2",
    "cypress-real-events": "^1.7.6",
    "cypress-wait-until": "^1.7.2",
    "start-server-and-test": "^1.15.2"
  },
  "lint-staged": {
    "**/packages/slice-machine/**/*.@(js|jsx|ts|tsx|)": [
      "prettier --write --ignore-unknown",
      "npm --prefix ./packages/slice-machine run lint:precommit"
    ]
  },
  "workspaces": [
    "packages/*",
    "e2e-projects/next2"
  ]
}
